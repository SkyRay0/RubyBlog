<nav class="navbar navbar-expand-lg bg-primary">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">
      <%= image_tag "anchor.png" %>
      <span class="text-light fw-bold">Fleets Of All Time</span>
    </a>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
        </li>
      </ul>
      <ul class="navbar-nav">
        <% if current_user %>
          <% if @current_user.username.nil? %>
            <%= puts @current_user.email %>
            <%= puts @current_user.id %>
            <li class="nav-item"> <%= link_to @current_user.email.split("@").first, "/user/#{@current_user.id}", class: "nav-link link-light link-underline-opacity-0" %></li>
          <% else %>
            <li class="nav-item"> <%= link_to @current_user.username, "/user/#{@current_user.id}", class: "nav-link link-light link-underline-opacity-0" %></li>
          <% end %>
          <li class="nav-item"> <%= button_to "Выйти", destroy_user_session_path, method: :delete, class: "btn btn-small btn-light mx-2" %></li>
        <% else %>
          <li class="nav-item"> <%= link_to "Зарегистрироваться", new_user_registration_path, class: "btn btn-small btn-light mx-2"  %></li>
          <li class="nav-item"> <%= link_to "Войти", new_user_session_path, class: "btn btn-small btn-light mx-2"  %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
<%= image_tag "gorshkov.png", class: "mx-auto d-flex p-2 bd-highlight rounded" %>
<nav class="navbar navbar-dark bg-primary justify-content-center">
  <a class="nav-link link-light active me-3 fw-bold" href="/?tag=HS">История</a>
  <a class="nav-link link-light active me-3 fw-bold" href="/">Главная</a>
  <a class="nav-link link-light active me-3 fw-bold" href="/?tag=AH">Альтернативная история</a>
  <a class="nav-link link-light active me-3 fw-bold" href="/?tag=AN">Аналитика</a>
  <% if user_signed_in? %>
    <a class="nav-link link-light active me-3 fw-bold" href="/?tag=FA">Любимые авторы</a>
  <% end %>
</nav>